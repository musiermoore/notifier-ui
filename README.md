# notifier-ui

Vue SPA for the notifier web client.

## UI Direction

- Russian is the default UI locale
- English is available as a secondary locale
- Auth and item management live here
- The desktop app can later reuse parts of this UI through Wails
